{
  "name": "hockeystick-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hockeystick trading backend — options order routing and market data",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node src/preflight.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
